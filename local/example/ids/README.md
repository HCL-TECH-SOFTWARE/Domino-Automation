Put your **Notes ID files** you want to use for Domino server setup into this directory. \
You can create a dedicated subdirectory for each Domino domain.

Example:
  - cert.id
  - server-alpha.id
  - admin.id
